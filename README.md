# v2 kis_scalper

This folder is a v2 rebuild driven by the guide and architect documents.
Scope (Python-only): single event loop, snapshot generation, minimal risk gate,
two entity presets, and paper simulation only. No news, no evolution.

Quick start (demo only):

```bash
python v2/main.py
```

Next steps:
- Replace demo events with real KIS adapters
- Persist logs to files/JSON
- Expand risk gate rules after the minimal set
